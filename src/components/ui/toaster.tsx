'use client';

import type { FC } from 'react';
import { Toaster as Sonner } from 'sonner';

const Toaster: FC = () => <Sonner richColors closeButton position="top-right" />;

export default Toaster;
